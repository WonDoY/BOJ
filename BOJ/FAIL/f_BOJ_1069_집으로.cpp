#include<cstdio>
typedef long double ld;

int main(){
	ld x, y; 
	ld d, t;
	while(scanf("%Lf%Lf%Lf%Lf", &d, &t, &x, &y) == 4){
		ld result = 0;

	}
	// 1을 움직이는 데 걸리는 시간 ? 
	// 둘 중 최대 값을 사용한다. 그러나 정확하게 D칸만 움직일수 있으므로 적절하게 섞어야 한다. 
	// 축을 기준으로 이동하는 것이 아니라 유동적으로 움직일 수 있다.
	// 그러니 일직선으로 하는 것은 어디로나 일직선이다. (원과같음)

	// 
	return 0;
}